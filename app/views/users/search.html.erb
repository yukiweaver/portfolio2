<% provide(:title, '検索ページ') %>
<h1>検索ページ</h1>
<div class="row">
  <div class="col-md-6 col-md-offset-3", style="text-align:center">
    <%= form_with url:'/search', method: :get, local: true do |f| %>
      <%= f.label :area_kbn, 'エリア' %>
      <%= f.select :area_kbn, ApplicationHelper::AREA_KBN, {include_blank: '選択してください'}, class: 'form-control' %>
      <%= f.submit 'マッチング', class: 'btn btn-primary btn-lg matching-btn', style: 'font-size:18px' %>
    <% end %>
  </div>
</div>

<!-- テーブル -->
<div class="alt-table-responsive">
  <table class="table"> 
    <thead>
      <tr>
        <th class="col-xs-3 col-ms-3 col-md-4 col-lg-4">名前</th>
        <th class="col-xs-3 col-ms-3 col-md-4 col-lg-4">Weather</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.email %></td>
          <td><%= link_to 'ユーザーページへ', '#' %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>